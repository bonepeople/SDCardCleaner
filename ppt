PPT 大纲
封面
    标题：项目问题分析与解决方案
    副标题：团队/项目名称
    日期
目录
    项目背景
    问题描述
    问题原因分析
    解决方案
    实施计划与步骤
    预期结果与收益
项目背景
    简要描述项目的目标和当前状态
    相关技术框架或背景知识（可以用图表简化说明）



问题描述
    网络请求逻辑
        请求端：生成随机密钥，使用 AES 加密数据，再用 RSA 公钥加密 AES 密钥（私钥在服务器端）。
        响应端：返回数据时，采用相同加密逻辑（AES + RSA）。
    现存问题
        RSA 私钥存储在安卓端。
        私钥存储位置不安全，存在被逆向工程或提取的风险。
        返回数据安全性无法保障。
    问题影响（技术、时间、资源等角度）

问题原因分析
    请求逻辑（无问题）
        客户端生成 AES 密钥 → 数据用 AES 加密 → AES 密钥用 RSA 公钥加密 → 服务器解密数据。
    返回逻辑（存在问题）
        服务器生成 AES 密钥 → 数据用 AES 加密 → AES 密钥用 RSA 私钥加密 → 客户端解密。
    问题核心
        客户端存储 RSA 私钥存在漏洞，易被攻击者利用。
解决方案
    改进数据加密逻辑
        响应数据采用对称加密（AES），避免客户端存储 RSA 私钥。
        使用预共享的加密密钥（可通过一次性密钥协商完成）。
    增强私钥保护
        将 RSA 私钥迁移到更安全的位置（如服务器端或硬件安全模块）。
        在安卓端引入安全模块（如 Android Keystore System）进行私钥存储。
    引入安全协议
        使用 TLS（传输层安全协议）加密整个通信过程，减少对返回数据加密的需求。

实施计划与步骤
    时间线或里程碑图
    任务分解与资源安排
预期结果与收益
    数据化的预期效果（例如性能提升、时间节省）
    风险评估及对策
总结
    强调解决方案的可行性
    呼吁领导支持或审批


===================2
封面
    标题：网络请求逻辑问题与解决方案
    副标题：改进加密逻辑以提升数据安全性
    日期

目录
    网络请求问题描述
    问题流程图
    解决方案说明
    实施步骤与预期效果

网络请求问题描述
    1. 现在正在使用的网络请求逻辑存在漏洞，涉及到客户端、前端、后端
    2. 该漏洞如果被攻击者利用会导致我们返回的数据被泄漏甚至是被篡改，导致系统整体安全性下降
    3. 此问题从APP创建之初就一直存在，已经持续了两年
    4. 最早发现于2024年4月份

问题流程图
    请求过程（无问题）：
        客户端生成随机 AES 密钥 → 使用 AES 加密数据 → AES 密钥用 RSA 公钥加密 → 服务器解密。
    返回过程（问题点）：
        服务器生成 AES 密钥 → 使用 AES 加密数据 → AES 密钥用 RSA 私钥加密 → 客户端解密。
        私钥泄露风险高，导致返回数据不安全。
解决方案
    预共享加密密钥方案：
        在请求发起时，客户端与服务器约定返回数据的 AES 密钥（预共享）。
        返回数据直接使用该密钥加密，无需使用 RSA 私钥。
    优点：
        无需在客户端存储 RSA 私钥，消除泄露风险。
        减少密钥交换步骤，提高效率。
实施步骤
    新方案开发与验证
        开发预共享加密密钥方案并验证逻辑正确性。
        进行单元测试、集成测试，确保新方案在不同场景下的兼容性和稳定性。
    迁移规划与步骤
        阶段1：双协议支持
            在服务器端同时支持新老加密方案，兼容现有客户端请求和返回逻辑。
            根据客户端版本或请求标识动态选择加密方案。
        阶段2：客户端更新
            发布包含新加密逻辑的客户端更新。
            在后台监控新客户端的更新进度和覆盖率。
        阶段3：切换到新方案
            当新客户端覆盖率达到目标（如 90%），逐步停用老方案。
            清理服务器端冗余逻辑，全面切换到新方案。
    成本评估与影响分析
        开发成本：评估新方案的开发时间及所需人力投入。
        迁移成本：监控双协议运行的额外资源消耗。
        风险评估：
            客户端更新率不高时，可能需要延长老方案的支持时间。
            双方案并存期间，可能增加维护复杂性和故障风险。
    后续工作
        定期检查新方案的运行效果，确保性能和安全性满足预期。
        收集用户反馈并优化客户端的使用体验。
预期效果
    消除客户端私钥泄露风险。
    提高返回数据的安全性，增强系统整体可靠性。